# Packaging and releases (not started)

Deferred by design — the brief's own milestone order (M4/M5) puts
packaging after a working M0-M2 core exists. Nothing to compare against
yet, so writing this now would just be restating the linked references
without evidence. Revisit once M1 produces a real binary to package.
